apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: db0
    user: ${INFLUXDB_ADMIN_USER}
    password: ${INFLUXDB_ADMIN_PASSWORD}
    url: http://influxdb:8086
    isDefault: true
    editable: true
